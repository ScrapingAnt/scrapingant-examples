{
  "comment": "Example ids (folder names under examples/) that run in CI. needs_api_key examples run only when the SCRAPINGANT_API_KEY secret is set.",
  "examples": [
    {
      "id": "scrape-dynamic-website-with-python",
      "needs_api_key": true,
      "note": "04 is skipped without the key"
    },
    {
      "id": "what-is-mcp-server",
      "needs_api_key": true,
      "note": "00/01/03 run without a key; 02 skipped without it"
    }
  ]
}