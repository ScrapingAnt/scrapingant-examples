{
  "comment": "Example ids (folder names under examples/) that run in CI. needs_api_key examples run only when the SCRAPINGANT_API_KEY secret is set.",
  "examples": [
    {
      "id": "scrape-dynamic-website-with-python",
      "needs_api_key": true,
      "note": "04 is skipped without the key"
    },
    {
      "id": "what-is-mcp-server",
      "needs_api_key": true,
      "note": "00/01/03 run without a key; 02 skipped without it"
    },
    {
      "id": "curl-with-proxy",
      "needs_api_key": false,
      "note": "starts local proxies; needs curl and outbound HTTPS"
    },
    {
      "id": "requests-ignore-ssl",
      "needs_api_key": false,
      "note": "starts a local self-signed HTTPS server; needs openssl and outbound HTTPS"
    },
    {
      "id": "python-parse-xml",
      "needs_api_key": false,
      "note": "generates a 24 MB file; the timing table is informational"
    },
    {
      "id": "wget-post-requests",
      "needs_api_key": false,
      "note": "starts a local echo server; needs wget and curl"
    },
    {
      "id": "wget-ignore-ssl",
      "needs_api_key": false,
      "note": "local self-signed and expired HTTPS servers; case 07 result depends on the wget TLS backend"
    },
    {
      "id": "beautifulsoup-cheatsheet",
      "needs_api_key": false,
      "note": "one public fixture fetch; case 11 installs bs4 into a scratch dir; speed table informational"
    },
    {
      "id": "requests-vs-httpx",
      "needs_api_key": false,
      "note": "starts a local hypercorn server (HTTP/1.1 + HTTP/2 over TLS); needs openssl and outbound HTTPS; 09 pip-installs httpx 1.0.dev6 into generated/; timing tables informational; 11 skipped without a key"
    },
    {
      "id": "pandas-read-html-table",
      "needs_api_key": false,
      "note": "fixture page served from GitHub Pages; two Wikipedia requests in 08 (403 with the default UA is the recorded result); 10 skipped without a key"
    },
    {
      "id": "python-proxy-server-proxy-py",
      "needs_api_key": false,
      "note": "local HTTP/HTTPS origins and proxy.py pool; requires curl and openssl; ports 8000, 8443, 8899-8905 must be free; random pool counts are informational"
    }
  ]
}
